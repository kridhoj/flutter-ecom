import 'package:get/get.dart';

class Dimenions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  static double pageViewHeight = screenHeight / 2.6;
  static double pageViewContainerHeight = screenHeight / 3.8;
  static double pageViewTextContainerHeight = screenHeight / 7.0;

  static double height10 = screenHeight / 84;
  static double height20 = screenHeight / 42;
}
